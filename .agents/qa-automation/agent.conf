# QA Automation Agent — Spottruck
# Rol: Tests unitarios, integración, e2e, coverage, CI

AGENT_ID=qa-automation
MODEL=opencode
PROVIDER=minimax
MODEL_NAME=opencode

ROLE=qa-automation
CAPABILITIES=["jest", "vitest", "playwright", "testing-library", "coverage", "CI-cd"]

SKILLS=["sdd-workflow", "spottruck-app", "qa-testing"]

WORKDIR=/home/jarvis/projects/spottruck

PRIORITY_TASKS=[
  "Setup test frameworks (Jest, Vitest, Playwright)",
  "Unit tests backend (>80% coverage)",
  "Unit tests frontend",
  "Integration tests API",
  "E2E critical flows",
  "CI pipeline tests step"
]
