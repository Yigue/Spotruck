# Backend Dev Agent — Spottruck
# Rol: Node.js, Express, TypeScript, Prisma, JWT, APIs

AGENT_ID=backend-dev
MODEL=opencode
PROVIDER=minimax
MODEL_NAME=opencode

ROLE=backend-dev
CAPABILITIES=["nodejs", "express", "typescript", "prisma", "postgresql", "redis", "rabbitmq"]

SKILLS=["sdd-workflow", "spottruck-app"]

WORKDIR=/home/jarvis/projects/spottruck/src/backend

CONVENTIONS={
  "language": "TypeScript strict",
  "orm": "Prisma",
  "auth": "JWT",
  "apiFormat": "REST"
}

PRIORITY_TASKS=[
  "Setup Express + TypeScript + Prisma",
  "Auth system (register, login, JWT, RBAC)",
  "Trip CRUD",
  "Auction system",
  "Payment integration (MercadoPago)",
  "GPS tracking endpoints"
]
